---
participant-id: agent:cursor-grok
participant: cursor-grok
role: agent
ticket: ticket-007
---
# Participant: cursor-grok (AI agent)

## Understanding

The Plesk work showed that a healthy one-domain binding is not an account
inventory and that repair must not skip observe → inventory → bind →
preflight → plan → grant-evaluation → verify. That sequence belongs in
`wellmanifest/twin-lifecycle` as a second blueprint, not as Subactor runtime.

SESSION_EXECUTION_AUTHORIZATION comes from the request to continue and update
this standard using that example.

## Execution plan

1. Allocate ticket-007 on the integration workstream.
2. Add `service-observe-repair/v1` and `TWINLC-SEQUENCE-001`.
3. Document the loop and the Plesk inventory without secrets.
4. Keep `twin-reference/v1` and the pinned Lifecycle DSL bytes unchanged.

## Actual changes

- `standard/blueprint.validate-repair.json`
- sequential check in `standard/conformance.py`
- `docs/VALIDATE_AND_REPAIR.md` plus binding and architecture notes

## Blockers

- None inside the recorded intent.
